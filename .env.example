# Copy this file to .env and update the value if your backend runs on a different port.
# The API Gateway (Spring Cloud Gateway) routes all microservice traffic.
# Default port is 8765 (configured in api-gateway/src/main/resources/application.properties).

VITE_API_BASE_URL=http://localhost:8765
